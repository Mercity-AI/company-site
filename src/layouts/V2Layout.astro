---
/**
 * Standalone shell for the landing page V2 experiment.
 *
 * Deliberately does not extend BaseLayout: V2 runs its own type stack
 * (Inter Tight / Inter / JetBrains Mono) and its own palette, and must not
 * be able to disturb the shipping site. No ClientRouter here either, so
 * the backdrop generators run on a plain page load.
 */
interface Props {
  title: string;
  description: string;
}

const { title, description }: Props = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex, nofollow" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="icon" href="/favicon.ico" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,300..600;1,9..144,300..600&family=Inter+Tight:wght@300..700&family=Inter:wght@300..600&family=JetBrains+Mono:wght@400;500&display=swap"
    />

    <style is:global>
      /* ══════════════════════════════════════════════════════════════
         V2 — scoped entirely to this layout.
         Palette from /design.html: indigo 243 (identity),
         teal 178 (measurement), amber 34 (signal), neutrals at 240.
         ══════════════════════════════════════════════════════════════ */
      :root {
        --indigo-100: hsl(243 47% 94%);
        --indigo-200: hsl(243 54% 87%);
        --indigo-300: hsl(243 62% 78%);
        --indigo-500: hsl(243 75% 59%);
        --indigo-600: hsl(243 69% 50%);
        --indigo-700: hsl(243 66% 41%);

        --teal-100: hsl(178 38% 92%);
        --teal-500: hsl(178 62% 42%);
        --teal-700: hsl(178 70% 27%);
        --teal-800: hsl(178 72% 21%);

        --amber-100: hsl(34 82% 92%);
        --amber-500: hsl(34 84% 53%);
        --amber-800: hsl(30 70% 28%);

        --n-0: #ffffff;
        --n-50: hsl(240 24% 99%);
        --n-100: hsl(240 18% 96%);
        --n-200: hsl(240 13% 91%);
        --n-300: hsl(240 10% 83%);
        --n-400: hsl(240 8% 65%);
        --n-500: hsl(240 7% 49%);
        --n-700: hsl(240 10% 27%);
        --n-800: hsl(240 12% 17%);
        --n-900: hsl(240 14% 10%);

        --ground: var(--n-50);
        --surface: var(--n-0);
        --ink: var(--n-900);
        --ink-2: var(--n-700);
        --ink-3: var(--n-500);
        --line: var(--n-200);
        --line-2: var(--n-300);

        --display: 'Inter Tight', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        /* Hero only. Everything else stays on the sans. */
        --hero-serif: 'Fraunces', Georgia, 'Times New Roman', serif;
        --sans: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        --mono: 'JetBrains Mono', ui-monospace, Menlo, monospace;

        --ease: cubic-bezier(0.22, 1, 0.36, 1);
        --shell: 1116px;
      }

      *,
      *::before,
      *::after {
        box-sizing: border-box;
      }

      html {
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
        scroll-behavior: smooth;
        color-scheme: light;
      }

      body {
        margin: 0;
        background: var(--ground);
        color: var(--ink);
        font-family: var(--sans);
        font-size: 14.4px;
        line-height: 1.6;
      }

      h1, h2, h3, h4, p, figure, ul {
        margin: 0;
      }
      ul {
        padding: 0;
        list-style: none;
      }
      a {
        color: inherit;
        text-decoration: none;
      }
      canvas {
        display: block;
      }
      :focus-visible {
        outline: 2px solid var(--indigo-500);
        outline-offset: 3px;
      }

      .shell {
        max-width: var(--shell);
        margin: 0 auto;
        padding-inline: 20.66px;
      }
      @media (max-width: 640px) {
        .shell {
          padding-inline: 13.77px;
        }
      }

      /* ── Reveal — the one curve ─────────────────────────────────── */
      .rv {
        opacity: 0;
        transform: translateY(24px);
        transition:
          opacity 600ms var(--ease),
          transform 600ms var(--ease);
        transition-delay: var(--rv-delay, 0ms);
      }
      .rv[data-from='left'] {
        transform: translateX(-24px);
      }
      .rv[data-from='right'] {
        transform: translateX(24px);
      }
      .rv[data-visible='true'] {
        opacity: 1;
        transform: none;
      }
      @media (prefers-reduced-motion: reduce) {
        .rv {
          opacity: 1;
          transform: none;
          transition: none;
        }
      }

      /* ── Shared type ────────────────────────────────────────────── */
      .eyebrow {
        display: block;
        font-family: var(--mono);
        font-size: 9px;
        font-weight: 500;
        letter-spacing: 0.18em;
        text-transform: uppercase;
        color: var(--indigo-600);
      }
      .h2 {
        margin-top: 14.4px;
        max-width: 22ch;
        font-family: var(--hero-serif);
        font-size: clamp(27px, 3.51vw, 42.3px);
        font-weight: 400;
        line-height: 1.14;
        letter-spacing: -0.02em;
        text-wrap: balance;
      }
      .lede {
        margin-top: 18px;
        max-width: 62ch;
        font-size: 14.85px;
        font-weight: 400;
        line-height: 1.76;
        color: var(--ink-3);
      }
      .band {
        border-top: 1px solid var(--line);
        padding-block: clamp(57.83px, 6.89vw, 90.88px);
      }
    </style>
  </head>

  <body>
    <header class="v2-nav" data-nav>
      <div class="v2-bar">
        <a href="/v2" class="v2-mark">Mercity<i>v2</i></a>
        <nav class="v2-links">
          <a href="#work">What we do</a>
          <a href="#who">Who we serve</a>
          <a href="#open">Open source</a>
          <a href="#tooling">Tooling</a>
        </nav>
        <a href="/contact" class="v2-cta">Start a conversation</a>
      </div>
    </header>

    <main>
      <slot />
    </main>

    <footer class="v2-foot">
      <div class="shell v2-foot-in">
        <span>Mercity Research — landing V2, experimental. The shipping page is at <a href="/">/</a>.</span>
        <span>Design system: <a href="/design.html">/design.html</a></span>
      </div>
    </footer>

    <style is:global>
      /* Full-bleed bar at rest. Past the trigger the background collapses
         out of the header and re-forms as a centred pill on the inner bar,
         which also loses 20% of its height. One transition, both states. */
      .v2-nav {
        position: sticky;
        top: 0;
        z-index: 60;
        padding-block: 0;
        background: hsl(240 30% 99% / 0.94);
        backdrop-filter: blur(16px) saturate(1.4);
        border-bottom: 1px solid var(--line-2);
        transition:
          padding-block 550ms var(--ease),
          background 550ms var(--ease),
          border-color 550ms var(--ease);
      }
      .v2-nav[data-scrolled='true'] {
        padding-block: 10.8px;
        background: transparent;
        backdrop-filter: none;
        border-bottom-color: transparent;
      }

      .v2-bar {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 21.6px;
        height: 61.2px;
        max-width: var(--shell);
        margin-inline: auto;
        padding-inline: 20.66px;
        background: transparent;
        border: 1px solid transparent;
        border-radius: 0;
        transition:
          height 550ms var(--ease),
          max-width 550ms var(--ease),
          padding-inline 550ms var(--ease),
          border-radius 550ms var(--ease),
          border-color 550ms var(--ease),
          background 550ms var(--ease),
          box-shadow 550ms var(--ease);
      }
      .v2-nav[data-scrolled='true'] .v2-bar {
        height: 48.6px;
        max-width: 792px;
        padding-inline: 19.8px;
        border-radius: 999px;
        border-color: var(--line-2);
        background: var(--n-0);
        backdrop-filter: blur(18px) saturate(1.4);
        box-shadow:
          0 18px 48px -20px hsl(240 14% 10% / 0.3),
          0 2px 7px -2px hsl(240 14% 10% / 0.09);
      }

      .v2-mark {
        font-family: var(--display);
        font-size: 18.9px;
        font-weight: 500;
        letter-spacing: -0.03em;
        transition: font-size 550ms var(--ease);
      }
      .v2-nav[data-scrolled='true'] .v2-mark {
        font-size: 15.3px;
      }
      .v2-mark i {
        font-family: var(--mono);
        font-style: normal;
        font-size: 8.55px;
        font-weight: 500;
        letter-spacing: 0.14em;
        text-transform: uppercase;
        color: var(--indigo-600);
        margin-left: 9px;
        vertical-align: 3px;
      }

      .v2-links {
        display: flex;
        gap: 30.6px;
        transition: gap 550ms var(--ease);
      }
      .v2-nav[data-scrolled='true'] .v2-links {
        gap: 23.4px;
      }
      .v2-links a {
        font-size: 12.15px;
        font-weight: 400;
        color: var(--ink-3);
        transition:
          color 300ms var(--ease),
          font-size 550ms var(--ease);
      }
      .v2-nav[data-scrolled='true'] .v2-links a {
        font-size: 11.25px;
      }
      .v2-links a:hover {
        color: var(--ink);
      }

      .v2-cta {
        padding: 8.1px 16.2px;
        border: 1px solid var(--ink);
        background: var(--ink);
        color: var(--n-50);
        border-radius: 0;
        font-size: 9.9px;
        font-weight: 500;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        white-space: nowrap;
        transition:
          background 300ms var(--ease),
          color 300ms var(--ease),
          border-color 300ms var(--ease),
          padding 550ms var(--ease),
          font-size 550ms var(--ease);
      }
      .v2-nav[data-scrolled='true'] .v2-cta {
        padding: 6.3px 13.5px;
        font-size: 9px;
      }
      .v2-cta:hover {
        background: var(--indigo-700);
        border-color: var(--indigo-700);
        color: #fff;
      }
      @media (max-width: 900px) {
        .v2-links {
          display: none;
        }
      }

      .v2-foot {
        border-top: 1px solid var(--line);
        background: var(--surface);
      }
      .v2-foot-in {
        display: flex;
        justify-content: space-between;
        gap: 18px;
        flex-wrap: wrap;
        padding-block: 36px;
        font-family: var(--mono);
        font-size: 9.45px;
        color: var(--n-400);
      }
      .v2-foot a {
        color: var(--indigo-600);
      }
    </style>

    <script is:inline>
      /* ── Reveal observer ──────────────────────────────────────── */
      (() => {
        const nodes = document.querySelectorAll('[data-rv]');
        if (!nodes.length) return;
        if (!('IntersectionObserver' in window)) {
          nodes.forEach((n) => n.setAttribute('data-visible', 'true'));
          return;
        }
        const io = new IntersectionObserver(
          (entries) => {
            entries.forEach((e) => {
              if (!e.isIntersecting) return;
              e.target.setAttribute('data-visible', 'true');
              io.unobserve(e.target);
            });
          },
          { rootMargin: '0px 0px -10% 0px', threshold: 0.01 }
        );
        nodes.forEach((n) => io.observe(n));
      })();
    </script>

    <script is:inline>
      /* ── Nav morph trigger ────────────────────────────────────── */
      (() => {
        const nav = document.querySelector('[data-nav]');
        if (!nav) return;
        let last = null;
        const update = () => {
          const on = window.scrollY > 28;
          if (on !== last) {
            last = on;
            nav.dataset.scrolled = on ? 'true' : 'false';
          }
        };
        update();
        window.addEventListener('scroll', update, { passive: true });
      })();
    </script>

    <script is:inline src="/v2-backdrops.js"></script>
  </body>
</html>
